        // display(n, adj);

        // int m = sc.nextInt();
        // adj = resizeAdj(adj, n, m);
        // n = m;

        // display(n, adj);

        // for (int i = 1; i <= n; i++) {
        // System.out.println(i + ": " + numberOfVertices(i, adj));
        // }

        // for (int i = 1; i <= n; i++) {
        // System.out.print(i + ": ");
        // getAdjacentVerticesOfNode(i, adj);
        // }